import "./SaveGameForm.css";

const SaveGameForm = ({ id }) => {

    return(
        <form>
        <h3>save game {id}</h3>
            <input id=""></input>
        </form>
    );
};

export default SaveGameForm;
